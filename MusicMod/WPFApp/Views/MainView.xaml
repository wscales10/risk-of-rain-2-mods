<Window x:Class="WPFApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:rulecontrols="clr-namespace:WPFApp.Controls.RuleControls"
		mc:Ignorable="d"
        Title="Rule Builder" Height="850" Width="1500"
		MouseDown="Window_MouseDown"
		SizeChanged="Window_SizeChanged"
		Deactivated="Window_Deactivated">
	<Window.Resources>
		<Style TargetType="Button">
			<Setter Property="Padding" Value="3"/>
			<Setter Property="Margin" Value="3"/>
			<Setter Property="MinWidth" Value="55"/>
		</Style>
	</Window.Resources>
	<Grid x:Name="masterGrid" Focusable="True">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal">
			<Button x:Name="BackButton" Click="BackButton_Click" IsEnabled="False">Back</Button>
			<Button x:Name="ForwardButton" Click="ForwardButton_Click" IsEnabled="False">Forward</Button>
			<Button x:Name="UpLevelButton" Click="UpLevelButton_Click" IsEnabled="False">Up</Button>
			<Button x:Name="HomeButton" Click="HomeButton_Click" IsEnabled="False">Home</Button>
			<Button x:Name="ImportButton" Click="ImportButton_Click">Import XML</Button>
			<Button x:Name="ExportButton" Click="ExportButton_Click" IsEnabled="False">Export XML</Button>
			<Button x:Name="NewButton" Click="NewButton_Click">New...</Button>
			<CheckBox x:Name="OfflineModeCheckbox" VerticalContentAlignment="Center" Margin="3" Padding="3">Offline Mode</CheckBox>
			<CheckBox x:Name="OpenLinksInAppCheckbox" Checked="OpenLinksInAppCheckbox_Checked" Unchecked="OpenLinksInAppCheckbox_Unchecked" VerticalContentAlignment="Center" Margin="3" Padding="3">Open links in app</CheckBox>
		</StackPanel>
		<rulecontrols:NewRuleControl Grid.Row="1" x:Name="newRuleControl" VerticalAlignment="Center"/>
	</Grid>
</Window>
