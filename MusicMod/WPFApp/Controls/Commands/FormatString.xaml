<UserControl x:Class="WPFApp.Controls.Commands.FormatString"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFApp.Controls.Commands"
             xmlns:commands="clr-namespace:WPFApp.ViewModels.Commands"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type commands:FormatString}}">
    <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding FilteredPropertyStrings}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type commands:IPropertyString}">
                    <local:PropertyString />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Menu Style="{StaticResource DiscreteMenu}" DataContext="{Binding MenuViewModel}" Visibility="{Binding HasItems, Converter={StaticResource ShouldBeVisibleConverter}}" />
    </StackPanel>
</UserControl>